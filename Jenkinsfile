pipeline {
    agent {
        label "Linux"
    }	

    stages {
        stage("first stage") {
            steps {
                echo "stage is executing"
            }
        }
    }
}
